#include<stdio.h>
#include<stdlib.h>

int main()
{
int ia,ib,ic,imax;
    printf("entrez la vl a:");
    scanf("%d",&ia);
    printf("entrez la vl b:");
    scanf("%d",&ib);
    printf("entrez la vl c:");
    scanf("%d",&ic);
if(ia>ib)

       printf("max %d \n",ia);
else
       printf("max %d \n",ib);
if(imax>ic)
      printf("max %d\n",imax);
else
       printf("max %d \n",ic);
return 0;
}
